name = "changelog"
description = "Generate changelogs from Git commits and changes"

task_prompt = """
Your task is to generate a changelog from Git commits and changes between two references.

## Workflow Steps:
1. Use git tools to get commits and diffs between the specified range
2. Use file_analyzer to understand the impact of key changes
3. Categorize changes by type and importance
4. Generate a structured changelog with clear sections

## Sample Workflows:

### Standard Changelog Generation:
1. `git_log(from, to)` - Get all commits in the range
2. `git_diff(from, to)` - Get overall diff summary
3. `analyze_file(path)` for significant changed files - Understand impact
4. Categorize changes: Features, Bug Fixes, Breaking Changes, Improvements
5. Generate structured changelog with clear sections and descriptions

### Feature-Focused Changelog:
1. `git_log(from, to)` - Get commits
2. `list_changed_files(from, to)` - See all modified files
3. `analyze_file()` for new feature files - Understand new capabilities
4. `git_diff()` for API/interface changes - Identify breaking changes
5. Focus on user-facing features and API changes

### Bug Fix Release:
1. `git_log(from, to)` - Get fix commits
2. `git_diff(from, to)` - See what was actually fixed
3. `analyze_file()` for fixed components - Understand fix scope
4. Focus on what issues were resolved and their impact

### Major Release:
1. `git_log(from, to)` - Get full commit history
2. `workspace_structure()` - Understand architectural changes
3. `analyze_file()` for core/breaking changes - Identify major changes
4. `git_diff()` for API surfaces - Document breaking changes
5. Structure by impact: Breaking Changes, Major Features, Minor Features, Fixes

## Changelog Format:
```markdown
# Changelog

## [Version] - YYYY-MM-DD

### 🚀 Features
- New feature descriptions with clear user benefits

### 🐛 Bug Fixes  
- What was broken and how it's fixed

### 💥 Breaking Changes
- API changes that require user action
- Migration guidance when needed

### ⚡ Improvements
- Performance, usability, or quality improvements

### 📚 Documentation
- Documentation updates and additions
```

## Guidelines:
- Focus on user-facing changes and their impact
- Group related changes together
- Use clear, non-technical language when possible
- Highlight breaking changes prominently
- Include relevant issue/PR references if available
""" 